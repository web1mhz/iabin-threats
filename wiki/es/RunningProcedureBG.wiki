#summary Pasos para utilizar la herramienta Biogeomancer.
#labels Destacado
#sidebar TableOfContents

[http://code.google.com/p/iabin-threats/wiki/RunningProcedureBG?wl=en http://iabin-threats.googlecode.com/svn/wiki/resources/gb.gif]
[http://code.google.com/p/iabin-threats/wiki/RunningProcedureBG?wl=es http://iabin-threats.googlecode.com/svn/wiki/resources/es.gif]

<wiki:toc />

= Introducción =

En este tutorial se describen los pasos para tener una instalación completamente funcional de la herramienta BioGeomancer.
  
===1 Configuración de archivos===
====Descripción====
En primer lugar, se deben establecer los valores para los parámetros del archivo de configuración XML _server_config.xml_.
 
Si no hay archivo de configuración, lo puede generar con los valores por defecto, mediante el script descrito en la sección de Procedimiento técnico. A continuación, puede editar los parámetros para establecer los valores correspondientes.  

La mayoría de los parámetros corresponden a las rutas de archivos y rutas de directorios. Los ejemplos para la configuración correcta de estos archivos se encuentran en el código fuente del proyecto. 

*Salidas:* Archivo de configuración: _server_config.xml_.

====Procedimiento Técnico====
Para generar el archivo de configuración por defecto, ejecute el script en java con los siguientes parámetros para la parte del servidor: 

 {{{java -cp ita.jar org.ciat.ita.bg.server.ServerConfig}}}

A continuación edite el archivo server_config.xml
  
   
===2 Georreferenciación===
====Descripción====
Para utilizar la herramienta de geo-rreferenciación, debe proporcionar el número de registros que desea procesar.

*Entradas:* Tabla MySQL con los registros de ocurrencias de la base de datos (que se seleccionan de la tabla especificada en server_config.xml), y la 

*Salida:* Los registros geo-rreferenciados se escriben a un lado de los registros originales, en la misma tabla, en los campos Blatitude, Blongitude, Uncertainty y is_fixed.

====Procedimiento Técnico====
Ejecutar en la base de datos la consulta sql del archivo llamado “QUERY_creates_table_georeferenced_records.sql” 

Ejemplo de línea de comando: 

 {{{mysql –u User –p portal < QUERY_creates_table_georeferenced_records.sql }}}

====Finalizar====
Una vez que la consulta se ha ejecutado, se crean las tablas georeferenced_records y temp_georeferenced_records. 

====Procedimiento Técnico====
Ejecute el script en java con los siguientes parámetros:

*Parámetros:* 
 
 {{{-Xmx<BYTES>}}} Máximo tamaño de bytes de almacenamiento dinámico de Java

*Línea de comando:*

 {{{java -XmxBYTES -cp ita.jar org.ciat.ita.bg.Biogeomancer}}}


===Local===

En modo local sólo necesita ejecutar una instancia.

*Parámetros:*
 {{{-Xmx<BYTES>}}} Máximo tamaño de bytes de almacenamiento dinámico de Java

*Línea de comando:*

 {{{java -XmxBYTES -cp ita.jar}}}

Ejemplo

 {{{java -Xmx1500m -jar ita.jar}}}

